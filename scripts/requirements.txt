# Default requirements — includes all dependencies
# For CI-only (daily heatmap refresh), use: requirements-ci.txt
# For local development (portrait generation), use: requirements-local.txt
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
Pillow>=10.4.0
numpy>=1.26.0
opencv-python>=4.10.0.84
rembg>=2.0.58

